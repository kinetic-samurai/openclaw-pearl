{
  "name": "openclaw-pearl",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw provider plugin for local Gemma edge models via LiteRT-LM",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "format": "prettier --write ."
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
